import { Configuration } from '../../sdk/index'

let config = new Configuration();
//config.basePath = "http://localhost:";

export default config;

export let COOKIE_SESSION_ID_NAME = "TYPI_SESSION_ID";